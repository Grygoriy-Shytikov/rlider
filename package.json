{
  "name": "rlider",
  "version": "1.0.0",
  "description": "test task",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "browser-sync start --server --files 'src/styles/*.css, src/styles/utils/*.css, src/styles/blocks/*.css, **/*.html, **/*.js'",
    "start": "run-p serve"
  },
  "author": "Grygoriy Shytikov",
  "license": "ISC",
  "devDependencies": {
    "browser-sync": "^2.27.7",
    "node-sass": "^6.0.1",
    "npm-run-all": "^4.1.5",
    "onchange": "^7.1.0"
  }
}
